import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cipavi.com',
  appName: 'CIPAVI+',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
